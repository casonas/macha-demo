// Re-export shim so imports can stay stable whether callers target the folder
// root or the nested implementation file.
export { QuestionCard } from './QuestionCard/QuestionCard';
export { default } from './QuestionCard/QuestionCard';
